var calc = {
   add: function (a, b) {
 console.log(a + b); /* to test if it runs properly*/
     return a + b;
   } ,

   sub: function (a, b) {
     return a - b;
   } ,
   divd: function(a, b) {
     return a / b;
   } ,
   mul: function(a, b) {
     return a * b;
   } ,
 };

 /* various calculations */

 calc.add();
 calc.sub();
 calc.divd();
 calc.mul();
